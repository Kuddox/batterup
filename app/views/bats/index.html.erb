<h1> Bats </h1>
<% @bats.each do |bat| %>
	<h2><%= bat.name %></h2>
	<h2><%= bat.description %></h2>
	<p><%= bat.price %></p>
	<p><%= bat.sku %></p>
	<%= link_to 'Show Bat', bat_path(bat.id) %>
	<p><%= link_to 'New Bat', new_bat_path %></p>
<% end %>
